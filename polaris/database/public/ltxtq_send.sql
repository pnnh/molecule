create function ltxtq_send(ltxtquery) returns bytea
    immutable
    strict
    parallel safe
    language c
as
$$
begin
-- missing source code
end;
$$;

alter function ltxtq_send(ltxtquery) owner to postgres;

