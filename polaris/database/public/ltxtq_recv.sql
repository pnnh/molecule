create function ltxtq_recv(internal) returns ltxtquery
    immutable
    strict
    parallel safe
    language c
as
$$
begin
-- missing source code
end;
$$;

alter function ltxtq_recv(internal) owner to postgres;

