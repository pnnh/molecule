create function _ltree_gist_options(internal) returns void
    immutable
    parallel safe
    language c
as
$$
begin
-- missing source code
end;
$$;

alter function _ltree_gist_options(internal) owner to postgres;

