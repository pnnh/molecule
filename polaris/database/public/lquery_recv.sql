create function lquery_recv(internal) returns lquery
    immutable
    strict
    parallel safe
    language c
as
$$
begin
-- missing source code
end;
$$;

alter function lquery_recv(internal) owner to postgres;

